[project]
name = "codexlight"
version = "0.1.0"
description = "A small top-center status island for wrapped Codex/CLI sessions."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
codexlight = "codexlight.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
